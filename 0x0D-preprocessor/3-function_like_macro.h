#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

#define ABS(x) ((x) < 0 ? -1 * (x) : (x))
/*if x is < 0, x is multiplied by -1 and value is returned*/
/*else the value is returned*/

#endif
